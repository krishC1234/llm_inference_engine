# Phase 1 dependencies (install on the T4 box; torch build must match its CUDA)
transformers>=4.40
accelerate>=0.30
sentencepiece>=0.2   # TinyLlama uses a Llama/SentencePiece tokenizer
numpy>=1.26
matplotlib>=3.8      # plotting per-step latency vs sequence length (step 8)
